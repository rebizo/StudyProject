plugins {
    id "java"
    id "application"

}

mainClassName='spring.BookwormOracle'
sourceCompatibility = '1.8'
[compileJava, compileTestJava]*.options*.encoding = 'UTF-8'

repositories {
    mavenCentral()
    maven { url "https://jitpack.io" }
}

dependencies {
    compile 'org.springframework:spring-context:5.0.1.RELEASE'
}

